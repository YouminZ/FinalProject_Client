'use strict';

/**
 * @ngdoc function
 * @name clientApp.controller:OptpoolCtrl
 * @description
 * # OptpoolCtrl
 * Controller of the clientApp
 */
angular.module('clientApp')
  .controller('OptPoolCtrl', function () {
    $(document).scrollTop(0);
  });
