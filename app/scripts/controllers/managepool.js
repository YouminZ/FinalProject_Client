'use strict';

/**
 * @ngdoc function
 * @name testProjectsApp.controller:ManagepoolCtrl
 * @description
 * # ManagepoolCtrl
 * Controller of the testProjectsApp
 */
angular.module('clientApp')
  .controller('ManagePoolCtrl', function () {
    $(document).scrollTop(0);
  });
